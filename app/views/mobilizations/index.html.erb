<p id="notice"><%= notice %></p>

<h1>Mobilizações</h1>

<table>
  <thead>
    <tr>
      <th>Título</th>
      <th>Categoria</th>
      <th>Descrição</th>
      <th>Mail content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mobilizations.each do |mobilization| %>
      <tr>
        <td><%= mobilization.title %></td>
        <td><%= mobilization.category %></td>
        <td><%= mobilization.description %></td>
        <td><%= mobilization.mail_content %></td>
        <td><%= link_to 'Abrir', mobilization %> </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
